# Rama master: 
  - Commit inicial con el contenido de la práctica anterior.
  - Se incluye y utiliza el modulo "Comparable", eliminando los métodos de sobrecarga de operadores de comparación anteriores.

# Rama "cristo-travis":
  - Se crea el fichero ".travis.yml" y se añaden las plataformas sobre las que se quieren probar el correcto funcionamiento.
  - Se combina la rama cristo-travis con la master

# Rama master:
  - Creación del fichero "Gemfile" y modificación de "Rakefile.rb"

# Correccion de errores:
  - Ausencia de Gemfile, en un commit.
  - La existencia de tildes daba errores en travis.
